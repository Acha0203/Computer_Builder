import React from 'react';
import Title from './Title';
import CPU from './CPU';
import GPU from './GPU';

const Layout = () => {
  return (
    <>
      <Title />
      <CPU />
      <GPU />
    </>
  );
};

export default Layout;
