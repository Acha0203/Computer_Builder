export type PCData = {
  type: string;
  partNumber: string;
  brand: string;
  model: string;
  rank: number;
  benchmark: number;
};

export type MemoryCadeData = {
  type: string;
  partNumber: string;
  brand: string;
  model: string;
  rank: number;
  benchmark: number;
  capacity: string;
};
